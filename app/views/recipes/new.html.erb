<%= form_for @recipe do |f| %>
  Title: <%= f.text_field :title %><br>
  <%= f.fields_for :ingredients do |n| %>
    <%= n.label :name %>
    <%= n.text_field :name %> <br>
    <%= n.label :quantity %>
    <%= n.text_field :quantity %> <br>
  <% end %>
  <%= f.submit %>
<% end %>
